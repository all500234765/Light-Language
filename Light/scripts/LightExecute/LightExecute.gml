/// @desc LightExecute
/// @arg Tokens
tokens = argument0;
index = 0;

while( index < ds_list_size(tokens) ) {
    var t = LightBuilder();
    index++;
    
    if( tokenType() == TokenType.NULL ) { break; }
}
